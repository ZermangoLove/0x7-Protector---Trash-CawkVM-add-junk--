using dnlib.DotNet.Emit;
using Helpers.Emulator;

namespace ek
{
	internal abstract class ej
	{
		internal abstract OpCode em { get; }

		internal abstract void en(EmuContext context, Instruction instr);
	}
}
